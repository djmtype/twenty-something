---
import ButtonLink from "@/components/ButtonLink.astro";
---

<ButtonLink variant="icon" class="navigation-toggle" :class="{ 'hidden': !isOpen }" :aria-expanded="isOpen" aria-controls="nav-primary__list" :aria-label="isOpen ? 'Close menu navigation' : 'Open menu navigation'" @click="isOpen = !isOpen">

    <template x-if="isOpen">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M5 5L19.1421 19.1421" stroke="currentColor" stroke-width="1.5"/>
      <path d="M19.1421 5L5 19.1421" stroke="currentColor" stroke-width="1.5"/>
    </svg>
    </template>
    <template x-if="!isOpen">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <line x1="4" y1="8.25" x2="20" y2="8.25" stroke="currentColor" stroke-width="1.5"/>
      <line x1="4" y1="15.75" x2="20" y2="15.75" stroke="currentColor" stroke-width="1.5"/>
    </svg>
    </template>
  
</ButtonLink>



<style is:global>
  .navigation-toggle {
    align-self: center;
  }

  .navigation-toggle svg {
    width: 100%;
    height: auto;
  }

  @media (--lg-n-above) {
    .navigation-toggle {
      display: none;
    }
  }

  .navigation-toggle[aria-expanded="true"] {
    color: var(--color-primary);
    background-color: transparent;
  }

  .no-js .navigation-toggle {
    display: none;
  }
</style>
