---
import settings from "@/data/settings.json"

interface Props {
	date: Date;

}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(settings.locale || 'en-US', {
			timeZone: "UTC",
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
