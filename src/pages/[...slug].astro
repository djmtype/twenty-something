---
import { getCollection, type CollectionEntry } from "astro:content"
import MarkdownWrapper from "@/layouts/MarkdownWrapper.astro"

export async function getStaticPaths() {
  const postEntries = await getCollection("postCollection")
  const pageEntries = await getCollection("pageCollection")

  const allEntries = [...postEntries, ...pageEntries]

  return allEntries.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }))
}

type AuthorData = {
  author: {
    slug: string;
  };
};

type Props = {
  entry: CollectionEntry<"postCollection" | "pageCollection"> & { data: AuthorData };
};

const { entry } = Astro.props
const { Content } = await entry.render();

---

<MarkdownWrapper frontmatter={entry}>
  <Content />
</MarkdownWrapper>
